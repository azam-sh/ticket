import React from "react";
import Slider from "../slider/Slider";

const Locations = () => {
  return (
    <div className="md:py-[20px] sm:px-[30px] py-[14px] sm:py-[30px] lg:py-[40px]">
      <h2 className="text-[22px] sm:text-[28px] text-center md:text-center lg:text-left text-bold md:text-[36px] mb-[13px] sm:mb-[20px] font-semibold">Локации</h2>
    <Slider />
    </div>
  );
};

export default Locations;
